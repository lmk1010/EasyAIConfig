- 终端模式：悬浮菜单、快捷键可收起、系统键盘输入；横竖屏不再遮挡状态栏
- 终端启动：ChatGPT 账号登录不再被 lucoo catalog 劫持
- 自动更新：默认走 Cloudflare R2（`download.cursorxyz.it.com`），GitHub Release 不再上传大安装包
- Android APK：支持从 R2 `latest.json` 检查 / 下载 / 安装更新
- 桌面端系统设置精简，并支持从 R2 直接下载各平台安装包
